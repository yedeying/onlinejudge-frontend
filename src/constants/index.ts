import { actionTypes } from './actionTypes';
import * as apiUrls from './apiUrls';
import * as common from './common';
// import * as error from './error';
// import * as route from './route';

/**
 * 每增加一个类型的常量集合，请在此引入并导出.
 * 方便外部以$constants为单一引用入口.
 * @liaowenhao
 */

export {
  actionTypes,
  apiUrls,
  common
  // error,
  // route
};
