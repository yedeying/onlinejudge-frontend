import * as React from 'react';

const { Component } = React;

export default class Navigationbar extends Component {
  render() {
    return (
      <div>
        Navigation
      </div>
    );
  }
}
